<h1>添加套餐卡<a href="javascript:void(0)" class="close">关闭</a></h1>
<div class="popup_body">
  <%= form_for :add_pcard,:url=>"/stores/#{params[:store_id]}/package_cards",:method=>:post,:html=>{:id=>"add_pcard",:multipart=>true} do %>
    <div class="popup_body_area">
      <%= render "card_form" %>
    </div>
  <% end %>
  <div class="btn_box_s"><button class="confirm_btn" onclick="check_add(this)">确定</button></div>
</div>